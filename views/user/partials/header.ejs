<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">

  <style>
    html { scroll-behavior: smooth; }
    .logomain { margin-left: 50px; }
    body { font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, 'Helvetica Neue', Arial, 'Noto Sans'; }
    .glass { background: rgba(255,255,255,0.7); backdrop-filter: saturate(160%) blur(10px); }
    .card-hover { transition: transform .35s ease, box-shadow .35s ease; }
    .card-hover:hover { transform: translateY(-6px); box-shadow: 0 22px 40px -16px rgba(0,0,0,0.25); }
    .img-zoom { transition: transform .6s cubic-bezier(.2,.6,.2,1); }
    .group:hover .img-zoom { transform: scale(1.06); }
    .btn { transition: transform .2s ease, box-shadow .3s ease, background-color .3s ease; }
    .btn:hover { transform: translateY(-1px) scale(1.02); box-shadow: 0 18px 30px -12px rgba(16,185,129,.45); }
    .shadow-soft { box-shadow: 0 8px 28px rgba(0,0,0,0.08); }
    .shadow-soft-lg { box-shadow: 0 16px 40px rgba(0,0,0,0.12); }
    .logomain img { width: auto; height: 40px; }
  </style>
</head>

<body class="bg-white text-gray-800">

  <!-- Header -->
  <header id="siteHeader" class="bg-white/90 glass sticky top-0 z-50 border-b border-gray-100">
    <div class="flex justify-between items-center h-16">
      <!-- Logo -->
      <div class="logomain"><img src="/assets/images/mainlogotz.png" alt=""></div>

      <!-- Nav -->
      <nav class="hidden md:flex items-center gap-8">
        <a href="#top-spots" class="text-sm text-gray-600 hover:text-gray-900 transition">Explore</a>
        <a href="#trending-food" class="text-sm text-gray-600 hover:text-gray-900 transition">Inspire</a>
        <a href="#" class="text-sm text-gray-600 hover:text-gray-900 transition">Booking Confirmation</a>
        <a href="#resorts" class="text-sm text-gray-600 hover:text-gray-900 transition">Best Deals</a>
        <a href="#map" class="text-sm text-gray-600 hover:text-gray-900 transition">Support</a>
      </nav>

      <!-- Icons -->
      <div class="flex items-center gap-3">
        <button class="text-gray-600 hover:text-gray-900 p-2 rounded-full hover:bg-gray-100 transition" aria-label="Search">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
          </svg>
        </button>
        <button class="text-gray-600 hover:text-gray-900 p-2 rounded-full hover:bg-gray-100 transition" aria-label="Account">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
          </svg>
        </button>
      </div>
    </div>
  </header>
